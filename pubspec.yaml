name: xiuxianapp
description: A cultivation-themed goal, habit, focus, and reward Flutter app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_local_notifications: ^21.0.0
  flutter_timezone: ^5.1.0
  hive_flutter: ^1.1.0
  intl: ^0.20.2
  table_calendar: ^3.2.0
  timezone: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
